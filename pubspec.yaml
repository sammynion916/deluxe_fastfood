name: restaurant_website
description: A Flutter web project for a restaurant website.
publish_to: 'none' 

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Add any additional dependencies here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    # Add your asset paths here, e.g., images or fonts
    - assets/fonts/